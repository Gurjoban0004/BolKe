<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_bg"
    android:fillViewport="true">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">
        <TextView style="@style/Bento.Headline" android:text="@string/translate_title" />
        <TextView android:id="@+id/translate_loading" style="@style/Bento.Body" android:layout_marginTop="24dp" android:text="@string/translate_loading" />
        <TextView
            android:id="@+id/translate_result"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:lineSpacingExtra="6dp"
            android:textColor="@color/app_text"
            android:textIsSelectable="true"
            android:textSize="26sp"
            android:textStyle="bold" />
        <TextView android:id="@+id/btn_translate_retry" style="@style/Bento.PillPrimary" android:layout_marginTop="20dp" android:text="@string/translate_retry" android:visibility="gone" />
        <LinearLayout android:id="@+id/translate_actions" android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginTop="24dp" android:orientation="vertical" android:visibility="gone">
            <TextView android:id="@+id/btn_translate_copy" style="@style/Bento.PillPrimary" android:text="@string/copy_punjabi" />
            <TextView android:id="@+id/btn_translate_speak" style="@style/Bento.PillGhost" android:layout_marginTop="10dp" android:text="@string/speak_punjabi" />
            <TextView android:id="@+id/btn_translate_another" style="@style/Bento.PillGhost" android:layout_marginTop="10dp" android:text="@string/try_another" />
            <TextView android:id="@+id/btn_translate_original" style="@style/Bento.PillGhost" android:layout_marginTop="10dp" android:text="@string/show_original" />
        </LinearLayout>
        <TextView android:id="@+id/translate_original" style="@style/Bento.Body" android:layout_marginTop="16dp" android:textIsSelectable="true" android:visibility="gone" />
        <TextView android:id="@+id/btn_translate_back" style="@style/Bento.PillGhost" android:layout_marginTop="18dp" android:text="@string/back" />
    </LinearLayout>
</ScrollView>
